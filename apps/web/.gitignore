# Generated by `pnpm --filter @dataroom/web exec msw init public/` for dev-mode mocking.
# Deliberately not committed: Vite copies `public/` verbatim into `dist`, and a service worker
# script in the production bundle is exactly what the CI check for MSW leakage looks for.
public/mockServiceWorker.js
